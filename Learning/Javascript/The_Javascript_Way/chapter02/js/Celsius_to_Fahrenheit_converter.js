// Write a programme that asks for a temperature in Celsius degrees, then displays it in Fahrenheit degrees.

let celsiusTemp = prompt("Enter Celsius temperature:");
let fahrenheitTemp = (celsiusTemp * 9 / 5) + 32;
console.log(`Temperature in Fahrenheit is ${fahrenheitTemp}.`);