/* Test circle.js
*/

const calcCircle = require("./circle.js");

const radius = 7;

console.log(`The circumference and area of a circle with a radius of ${radius} are: ${calcCircle.circumference(radius)} and ${calcCircle.area(radius)} respectively.`);